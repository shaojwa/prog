#include <limits.h>
#include <stdio.h>

int main() {
  long len;
  printf("sizeof(long) = %lu\n", sizeof(long));
  printf("sizeof(ULONG_MAX) = %lu\n", sizeof(ULONG_MAX));
  printf("ULONG_MAX = %lu\n", ULONG_MAX);
  return 0;
}
